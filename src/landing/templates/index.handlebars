<!DOCTYPE html>
<head>
  <meta charset="utf-8">
  <title>when.st</title>
  <link rel="stylesheet" type="text/css" href="{{ asset "pure-min.css" }}">
  <link rel="stylesheet" type="text/css" href="{{ asset "main.css" }}">
  <link rel="shortcut icon" type="image/png" href="{{ asset "favicon.png" }}">
</head>
<body>
  <div id="layout" class="pure-g">
    <div class="pure-u-1">
      <form method="GET" action="https://slack.com/oauth/authorize">
        <input type="hidden" name="client_id" value="{{client_id}}">
        <input type="hidden" name="redirect_uri" value="{{ absolute "authSlack" }}">
        <input type="hidden" name="state" value="{{ state }}">
        <input type="hidden" name="scope"
          value="team:read users.profile:read users.profile:write dnd:write emoji:read"
        >
        <button type="submit">login</button>
      </form>
    </div>
  </div>
</body>
